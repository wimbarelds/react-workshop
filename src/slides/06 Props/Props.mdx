import { Code } from 'wb-slides';

import exampleProps1Tsx from './examples/ExampleProps1.tsx?raw';

# Props: Data doorgeven

Net als dat we in JSX aan HTML attributen kunnen meegeven (ie: `<a href="...">`), kunnen we ook
eigenschappen meegeven aan componenten. Bij componenten heet dit `"props"`.

<Code code={exampleProps1Tsx} />
