import { Code } from 'wb-slides';

import exampleChildrenTsx from './examples/ExampleChildren.tsx?raw';

# De "children" prop

Er is een speciale "prop", namelijk `children`. Deze kan je zo meegeven:

<Code code={exampleChildrenTsx} />
