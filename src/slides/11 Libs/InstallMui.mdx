import { Code, Prose } from 'wb-slides';

import { fontImports } from './content';

# MUI Installeren

Voer de volgende commands uit in je terminal

<code className="cmd block">
  {'npm install @mui/material @emotion/react @emotion/styled'}
  {'npm install @fontsource/roboto'}
</code>

En zet de volgende regels boven in je `main.tsx`:

<Code code={fontImports} />
