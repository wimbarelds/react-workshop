import { Code } from 'wb-slides';

import { buttonImport, ButtonLink } from './content';

# Using `<Button>`

De volgende: Button.

Deze is simpel, en er is zelfs simpele <ButtonLink>documentatie</ButtonLink>!

<Code code={buttonImport} />
