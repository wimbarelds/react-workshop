import { Prose } from '../../shared/Prose';

export function UsingList() {
  return (
    <Prose>
      <h1>
        Using <code>{'<List>'}</code> and <code>{'<ListItem>'}</code>
      </h1>
    </Prose>
  );
}
