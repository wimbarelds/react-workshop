import { Code } from 'wb-slides';

import exampleList from './examples/ExampleList.js?raw';

# Een lijst renderen

De volgende taak is om je takenlijst te maken op basis van data.

Sla een aantal taak-items op in een variable, en render die lijst

Voorbeeld van hoe je lijst er uit zou kunnen zien:

<Code code={exampleList} />
(Copy paste gerust)
