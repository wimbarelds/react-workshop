import { Prose } from '../../shared/Prose';

export function HideIfDone() {
  return (
    <Prose>
      <h1>Voltooide taken verbergen</h1>
      <p>
        Nu hebben we een lijst, en we kunnen JavaScript gebruiken in JSX. Laten we dat combineren,
        en daarmee de taken die al voltooid zijn verbergen.
      </p>
      <p>Er zijn een aantal manieren om dit om te lossen.</p>
    </Prose>
  );
}
