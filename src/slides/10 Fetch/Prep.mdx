import { Code } from 'wb-slides';

import { LoadAndShow } from './LoadAndShow';

# Mini-party

Met die laatste slide hebben we de belangrijkste onderwerpen gehad. Alles wat hierna nog
komt is mooi meegenomen.

Nu gaan we naar data-loading. Om dat te doen een klein beetje voorbereiding.

## Download, of copy paste

`db.json` <LoadAndShow url="./db.json" render={(content) => <Code code={content} />} />

<a href="./db.json" download="db.json" target="_blank" children="Download" />

En zet het bestand in de root van je project.

Voer daarna (in een losse terminal) uit: <code className="cmd">npx json-server db.json</code>

Je zou zo iets moeten zien:

<img
  className="contrast-200 rounded-xl"
  src="json-server.jpg"
  alt="Terminal output of json-server"
/>
