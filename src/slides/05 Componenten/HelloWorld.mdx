# Hello World

Zoals wel vaker, we gaan eerst een simpel Hello World component maken.

- Maak een bestand `"HelloWorld.tsx"` in je `src` directory.
- Voeg je eerste component toe in dat bestand. (vergeet hem niet te exporteren!)
- Importeer en gebruik jouw component in `"src/App.tsx"`.
