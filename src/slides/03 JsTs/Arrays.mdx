import { Code } from 'wb-slides';

import JsxFilter from './examples/JavascriptFunctionCallback.js?raw';
import JsxMap from './examples/JavascriptMap.js?raw';

# Arrays

## Array Methoden (map & filter)

Arrays zijn een datastructuur in JavaScript voor een reeks waarden / een lijst. De `map` en `filter`
methoden zijn hulpmiddelen om met arrays te werken.

### `map()`

De `map()` methode maakt een nieuwe array aan door een functie uit te voeren op
elk element van de originele array.

<Code code={JsxMap} />

### `filter()`

De `filter()` methode maakt een nieuwe array aan met alle elementen die slagen voor de test die door
de opgegeven functie wordt geïmplementeerd.

<Code code={JsxFilter} />
